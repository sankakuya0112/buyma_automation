# BUYMA 自動出品ツール：利益最優先の実装指示書

このファイルは Claude Code に貼り付けて実行させるための指示書です。
**利益を出すこと**を最優先とし、最短で売上が立つ状態に持っていきます。

---

## プロジェクトのゴール

BUYMA での月次利益を最大化するため、以下を達成する自動化システムを構築する。

1. 1サイト（baseblu.com）の商品データを自動取得
2. 正確な利益計算で「売れば確実に利益が出る価格」を算出
3. BUYMA 取り込み用 CSV を生成
4. まずは手動アップロードで売上を立てる
5. 売上が立ったら自動出品・在庫管理・サイト追加で拡大する

---

## 現状の把握（最初にやってほしいこと）

あなた（Claude Code）は最初に以下を確認してください。

1. `/Users/mgakusei/buyma_automation/` 配下の現在のコード構成を把握
2. `scripts/run_pipeline.py` がどこまで動くか確認（モックデータで十分）
3. `app/core/models.py` のDB設計を把握
4. 既存の `CLAUDE.md` に書かれている制約（サーバーから外部サイトにアクセス不可、等）を必ず守る
5. 確認結果を TodoList にまとめてからユーザーに報告する

---

## フェーズ別タスク（この順で実装）

### フェーズ1：利益が出る最小構成（最優先・これだけで売上ゼロ→プラスに）

**目標：baseblu の商品を BUYMA に手動出品して、最初の1件を売る**

#### タスク 1-1：価格計算ロジックの完成
- **やること**：以下を全て反映した売価計算関数を作る
  - 仕入値（EUR/USD）→ JPY 換算（為替レートは固定 or API 取得）
  - VAT 還付（欧州サイトなら仕入値の約 16.7% を控除、日本向け輸出のため）
  - 関税（カテゴリ別の税率マスタを持つ。皮革製品・衣類・バッグで異なる）
  - 国際送料（商品重量 × 1kg あたり単価、または固定値）
  - BUYMA 手数料（売価の 5.5%〜7%）
  - 決済手数料（約 3%）
  - 目標利益率（デフォルト 20%、`config.json` で変更可能）
- **完了条件**：
  - 仕入値を入れると売価と利益額が出力される
  - 最低利益額を下回る商品は「出品しない」フラグが立つ
  - ユニットテストでいくつかのパターンで検算が通る
- **ファイル**：`app/core/pricing.py`

#### タスク 1-2：baseblu スクレイパーの安定化
- **やること**：baseblu の商品一覧ページ・詳細ページから以下を取得
  - 商品名（原文）、ブランド、型番、カテゴリ
  - 価格（通貨込み）、割引前価格
  - サイズ一覧、色、在庫状況
  - 画像URL（最低3枚）、商品説明（原文）
- **注意**：サーバーからはアクセス不可。ローカル（Mac）実行前提で作る
- **完了条件**：`--test` モードではモックデータで動作確認、実行時は実サイトから取得
- **ファイル**：既存の scraper を拡張

#### タスク 1-3：翻訳モジュール
- **やること**：商品名・商品説明を日本語化
- **最初の実装**：Google Translate API、DeepL API、または無料の機械翻訳ライブラリ
- **API キー**：`.env` にユーザーが設定する前提で、Claude Code はコードだけ書く
- **品質向上は後回し**：まずは動けばよい
- **ファイル**：`app/core/translator.py`

#### タスク 1-4：重複チェック機能
- **やること**：既に出品した商品を SQLite DB で管理し、同じ商品（URL または型番）が再出品されないようにする
- **完了条件**：同じ URL の商品は 2 回目の実行ではスキップされる
- **ファイル**：`app/core/dedup.py`

#### タスク 1-5：BUYMA 取り込み用 CSV 出力
- **やること**：以下の 34 列を持つ CSV を出力する（PLUSELECT_TOOL から判明した BUYMA 標準フォーマット）

```
item_img_folder, item_name, item_brand, item_model, item_category,
item_comment, item_size_color, item_deadline, item_url, item_buyplace,
item_shop, item_sendplace, item_announce, color, size, item_season,
item_tag, item_thema, item_sell_price, item_pub_price, item_delivery,
item_stock, item_sku, item_duty, item_memo, item_price, item_currency,
item_no_cur_price, item_deli_price, item_vatoff, item_profit, item_keywords,
item_topic, item_image
```

- **完了条件**：
  - BUYMA の CSV 取り込み画面にアップロード可能な形式
  - UTF-8 with BOM、カンマ区切り
  - 画像URL は `|` で区切って複数入れる
- **ファイル**：`app/core/csv_writer.py`

#### タスク 1-6：エンドツーエンド統合テスト
- **やること**：モックデータで フェーズ1 全体が通ることを確認
- **完了条件**：`python3 scripts/run_pipeline.py --test` が成功し、CSVが生成される
- **追加**：実サイトでも少量（5件）で動くか Mac でテストしてもらうための手順書を `docs/PHASE1_TEST.md` に書く

---

### フェーズ2：売上拡大と損失回避（売上が立った後）

**目標：手動作業を減らし、キャンセル・違反を防ぐ**

#### タスク 2-1：BUYMA 自動出品
- Playwright を使って BUYMA にログインし、CSV をアップロード
- 既存の Chromium パス `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` を参照
- ログイン情報は Mac 上の `.env` または `config.json` から読む（チャットには絶対に出さない）

#### タスク 2-2：在庫自動チェック・停止
- **重要度：最高**。キャンセルは BUYMA 評価を壊滅的に下げる
- 仕入れ元で売り切れた商品を毎日チェック → BUYMA 側で自動的に出品停止
- スケジュール実行（`scripts/schedule_stock_check.py`）

#### タスク 2-3：価格追従
- 仕入れ元が値下げ → BUYMA 側も連動して値下げ
- 仕入れ元が値上げ → BUYMA 側も連動して値上げ（利益率維持）
- ただし 1日1回までなど、頻度制限は付ける

#### タスク 2-4：エラー通知
- 出品失敗・ログイン失敗・スクレイピング失敗時にメール／LINE 通知
- 最初はメールで十分

---

### フェーズ3：スケール拡大（さらに利益を増やす）

#### タスク 3-1：対応サイト追加
- 優先順位：`mytheresa`、`farfetch`、`cettire`、`italist`（高マージン・日本向け発送可のサイトから）
- 各サイトのスパイダーを独立して実装（複数エージェント並列開発の候補）

#### タスク 3-2：画像加工の自動化
- 商品メイン画像に自分のショップロゴを追加
- 背景を透過または統一色に変換
- `Pillow` または `OpenCV` を使用

#### タスク 3-3：売上分析ダッシュボード
- どのカテゴリ・ブランド・価格帯が売れているかを集計
- 売れない商品を自動的に出品停止

---

## 実装時の絶対ルール

1. **サーバー側でできないことは絶対にやろうとしない**
   - baseblu.com, buyma.com への直接アクセス → Mac で実行
   - playwright のブラウザダウンロード → 既存の Chromium を使う
2. **ログイン情報・APIキーはチャットに出さない**
   - 全て Mac 上の `.env` または `config.json` に保存
   - `.gitignore` に追加済みであることを確認してから作業
3. **モックデータで先にテスト**
   - 実行コストをかける前に `--test` モードで動作確認
4. **各タスク完了ごとに git commit & push**
   - Mac から push は不要。Claude Code サーバーで push する
   - コミットメッセージは「phase1-1: 価格計算ロジック実装」のように明確に
5. **TodoList を使って進捗を明示**
   - ユーザーが進捗を常に把握できるようにする
6. **完了条件を満たさない限り「完了」としない**
   - テストが通る・エラーハンドリングが入っている・ドキュメントがある、の3点が揃ってから次へ

---

## 複数エージェント運用時の役割分担（推奨）

### 方式A：レビュー分担（初心者向け・MVP段階）
- **実装エージェント**：コードを書く
- **検証エージェント**：動作確認・セキュリティチェック・コード品質確認

### 方式B：サイト別分担（スケール段階・フェーズ3で活用）
- **共通エージェント**：ベーススパイダー・共通ライブラリ担当
- **サイト担当エージェント A**：baseblu 専属
- **サイト担当エージェント B**：mytheresa 専属
- **サイト担当エージェント C**：farfetch 専属
- 各エージェントは `git worktree` で独立ブランチで作業

### 方式C：パイプライン分担（高度化段階）
- **スクレイパー担当**
- **価格計算担当**
- **翻訳担当**
- **画像加工担当**
- **出品担当**

---

## 最初にユーザーに確認してほしいこと

Claude Code がこの指示書を受け取ったら、まず以下をユーザーに確認してから作業を始めてください。

1. 目標利益率は何 % にしますか？（デフォルト20%）
2. 最低利益額はいくらですか？（これ以下なら出品しない。例：2000円）
3. 翻訳サービスはどれを使いますか？（Google Translate / DeepL / 無料ライブラリ）
4. 配送代行業者はどこですか？（送料計算に使用）
5. 仕入れ用の基準通貨は何ですか？（EUR / USD / GBP）
6. 関税は簡易計算でよいですか？それとも HSコード別の精密計算が必要ですか？

---

## 完了時のユーザーへの成果物

フェーズ1完了時、以下を `/Users/mgakusei/buyma_automation/docs/` に納品してください。

- `PHASE1_DONE.md`：フェーズ1 で実装した機能一覧・使い方
- `PHASE1_TEST.md`：Mac でのテスト手順（どのコマンドを叩けばよいか）
- `PROFIT_CALCULATION.md`：利益計算のロジック説明（ユーザーが検算できるように）
- `BUYMA_CSV_SPEC.md`：CSV 34 列の意味と記入ルール

---

## 備考：ユーザーはプログラミング初心者

- コード断片を説明するときは「何をしているか」を日本語で添える
- エラーが出たらコマンド実行結果を貼り付けてもらう方針でサポートする
- 専門用語を使うときは初出で簡単な説明を付ける
