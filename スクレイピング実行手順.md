# BaseBlu スクレイピング実行手順

## Claude Code で実行する方法

### 1. Claude Code を起動する

ターミナルを開いて、このプロジェクトフォルダに移動します：

```bash
cd ~/Documents/Claude/Projects/BUYMAオートメーション
claude
```

### 2. Claude Code に以下のように指示する

```
python3 scripts/baseblu_sales_to_csv.py を実行して、結果をCSVに保存してください
```

---

## 直接ターミナルで実行する方法（Claude Code なしでも可）

### 必要なライブラリのインストール（初回のみ）

```bash
pip3 install requests
```

### スクリプト実行

```bash
cd ~/Documents/Claude/Projects/BUYMAオートメーション
python3 scripts/baseblu_sales_to_csv.py
```

### 出力先

`outputs/reports/YYYY-MM-DD_baseblu_sales_products_sorted.csv` に保存されます。

---

## スクリプトの内容（baseblu_sales_to_csv.py）

- **取得先**: https://www.baseblu.com/en-us/collections/sales/products.json
- **取得内容**: 商品名・ブランド・セール価格・元値・割引率・在庫状況・画像URL・商品URL
- **ソート**: 割引率が高い順

---

## なぜ Cowork から直接実行できないのか

Cowork（Claude デスクトップアプリ）の内部サンドボックス環境は、外部サイトへのアクセスが一部制限されています。Claude Code（ターミナルのCLI）はあなたのMacのネットワークをそのまま使うため、制限なく実行できます。
